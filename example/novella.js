var Novella = (function (Vue) {
  'use strict';

  
  console.log("Generated by novella at 10/23/2016, 3:07:26 PM.");
  var data = {"title":"Ducks","pages":[{"index":0,"filename":"1-Mallard.png","location":"pages","mtime":"2016-10-22T21:28:22.000Z","width":450,"height":450},{"index":1,"filename":"2-Blue Billed.png","location":"pages","mtime":"2016-10-22T21:30:51.000Z","width":450,"height":450},{"index":2,"filename":"3-Peking.png","location":"pages","mtime":"2016-10-22T21:32:46.000Z","width":450,"height":450},{"index":3,"filename":"4-Mandarin.png","location":"pages","mtime":"2016-10-22T21:33:51.000Z","width":450,"height":450}]};

  Vue = 'default' in Vue ? Vue['default'] : Vue;

  var image = [".png",".gif",".jpg"];
  var formats = {
  	image: image
  };

  function isImage(filename) {
    var imageTypes = new Set(formats.image);
    var split = filename.split('.');
    var type = '.' + split[split.length - 1];
    return imageTypes.has(type);
  }

  var page = {
    index: Number,
    filename: String,
    location: String,
    mtime: Date,
  };

  var pages = Array;

  var store$1 = {
    title: String,
    pages,
    pageroute: String,
    pagenum: Number,
  };

  var Page = { template: "<div class=\"page\"><div><img v-if=\"isImage()\" :src=\"getSource()\"></div><span>{{ new Date(page.mtime).toLocaleString() }}</span></div>",
    props: { page },
    methods: {
      isImage() { return isImage(this.page.filename); },
      getSource() {
        return `${this.page.location}/${this.page.filename}`;
      },
    },
  };

  var PageList = { template: "<ul class=\"page-list\"><li v-for=\"n in Math.min(length, 10)\" :data-selected=\"isSelected(n)\" :key=\"getSource(n)\"><a :href=\"getRoute(n)\"><img v-if=\"isImage(n)\" :src=\"getSource(n)\"><div class=\"pagenum\">{{ n }}</div></a></li></ul>",
    props: { store: store$1 },
    computed: {
      length() { return this.store.pages.length; },
    },
    methods: {
      isImage(n) {
        var page$$1 = this.store.pages[n-1];
        return isImage(page$$1.filename);
      },
      isSelected(n) {
        return n === this.store.pagenum;
      },
      handleClick(e) {
        this.clickPage(e.currentTarget.dataset.index);
      },
      getSource(n) {
        var page$$1 = this.store.pages[n-1];
        return `${page$$1.location}/${page$$1.filename}`;
      },
      getRoute(n) {
        var route = this.store.pageroute;
        if (!route) return null;
        return `${route}/${n}`;
      },
      getListItem(n) {
        return this.$el.querySelectorAll('li')[n-1];
      },
    },
  };

  var PageControl = { template: "<div class=\"page-control\"><a class=\"prev\" :href=\"getRoute(store.pagenum - 1)\" :disabled=\"!canPrev()\">prev ([ q) </a><a class=\"next\" :href=\"getRoute(store.pagenum + 1)\" :disabled=\"!canNext()\">(] w) next</a></div>",
    props: { store: store$1 },
    data: () => ({
      animateSidebar: null,
      hideSidebar: false,
    }),
    components: {
      Page,
      PageList,
    },
    methods: {
      canPrev() { return this.store.pagenum > 1; },
      canNext() { return this.store.pagenum < this.store.pages.length; },
      getRoute(n) {
        var route = this.store.pageroute;
        if (!route) return null;
        return `${route}/${n}`;
      },
    },
  };

  function restrict(value, min, max) {
    var num = Number(value);
    num = Math.min(num, max);
    num = Math.max(num, min);
    return num;
  }

  function scrollToElement(parent, element) {
    var parentRect = parent.getBoundingClientRect();
    var elementRect = element.getBoundingClientRect();

    if (elementRect.bottom > parentRect.bottom) {
      return element.scrollIntoView(false);
    }

    if (elementRect.top < parentRect.top) {
      element.scrollIntoView(true);
    }
  }

  var App = { template: "<div class=\"app\"><transition name=\"slide\"><div class=\"sidebar\" v-if=\"!hideSidebar\"><h1>{{ store.title }}</h1><page-list :store=\"store\" ref=\"pageList\"></page-list></div></transition><div class=\"content\"><button class=\"sidebar-toggle\" v-on:click=\"toggleSidebar()\">{{ hideSidebar ? 'show menu' : 'hide menu' }}</button><page :page=\"getPage(store.pagenum)\"></page><page-control :store=\"store\"></page-control></div></div>",
    props: { store: store$1 },
    data: () => ({
      animateSidebar: null,
      hideSidebar: false,
    }),
    components: {
      Page,
      PageList,
      PageControl,
    },
    methods: {
      getPage(n) { return this.store.pages[n - 1]; },
      toggleSidebar() {
        this.hideSidebar = !this.hideSidebar;
        if (this.hideSidebar) this.animateSidebar = 'close';
        else this.animateSidebar = 'open';
      },
      onAnimationEnd() {
        this.animateSidebar = null;
      },
      scrollToSelected() {
        var pageList = this.$refs.pageList;
        if (!pageList) return;

        var selected = pageList.getListItem(this.store.pagenum);
        scrollToElement(this.$el, selected);
      },
    },
    mounted: function mounted() {
      this.scrollToSelected();
    },
    updated: function updated() {
      this.scrollToSelected();
    },
  };

  // from: http://krasimirtsonev.com/blog/article/A-modern-JavaScript-router-in-100-lines-history-api-pushState-hash-url

  var Router = {
      routes: [],
      mode: null,
      root: '/',
      config: function(options) {
          this.mode = options && options.mode && options.mode == 'history'
                      && !!(history.pushState) ? 'history' : 'hash';
          this.root = options && options.root ? '/' + this.clearSlashes(options.root) + '/' : '/';
          return this;
      },
      getFragment: function() {
          var fragment = '';
          if(this.mode === 'history') {
              fragment = this.clearSlashes(decodeURI(location.pathname + location.search));
              fragment = fragment.replace(/\?(.*)$/, '');
              fragment = this.root != '/' ? fragment.replace(this.root, '') : fragment;
          } else {
              var match = window.location.href.match(/#(.*)$/);
              fragment = match ? match[1] : '';
          }
          return this.clearSlashes(fragment);
      },
      clearSlashes: function(path) {
          return path.toString().replace(/\/$/, '').replace(/^\//, '');
      },
      add: function(re, handler) {
          if(typeof re == 'function') {
              handler = re;
              re = '';
          }
          this.routes.push({ re: re, handler: handler});
          return this;
      },
      remove: function(param) {
          for(var i=0, r; i<this.routes.length, r = this.routes[i]; i++) {
              if(r.handler === param || r.re.toString() === param.toString()) {
                  this.routes.splice(i, 1);
                  return this;
              }
          }
          return this;
      },
      flush: function() {
          this.routes = [];
          this.mode = null;
          this.root = '/';
          return this;
      },
      check: function(f) {
          var fragment = f || this.getFragment();
          for(var i=0; i<this.routes.length; i++) {
              var match = fragment.match(this.routes[i].re);
              if(match) {
                  match.shift();
                  this.routes[i].handler.apply({}, match);
                  return this;
              }
          }
          return this;
      },
      listen: function() {
          var self = this;
          var current = self.getFragment();
          var fn = function() {
              if(current !== self.getFragment()) {
                  current = self.getFragment();
                  self.check(current);
              }
          };
          clearInterval(this.interval);
          this.interval = setInterval(fn, 50);
          return this;
      },
      navigate: function(path) {
          path = path ? path : '';
          if(this.mode === 'history') {
              history.pushState(null, null, this.root + this.clearSlashes(path));
          } else {
              window.location.href = window.location.href.replace(/#(.*)$/, '') + '#' + path;
          }
          return this;
      },
  };

  /* globals data */

  var total = data.pages.length;
  var local = window.localStorage;

  var restrictPage = (n) => restrict(n, 1, total);

  var store = {
    title: data.title,
    pages: data.pages,
    pageroute: '#/page',
    pagenum: restrictPage(local.getItem('pagenum')),
  };

  Vue.component('app', App);
  Vue.component('page', Page);
  Vue.component('page-list', PageList);

  Router.config({ mode: 'hash' });
  Router.add(/page\/([\d\w]*)/, (pagenum) => {
    if (pagenum === 'last') pagenum = total;
    pagenum = restrictPage(pagenum);
    store.pagenum = pagenum;
    local.setItem('pagenum', pagenum);
  })
  .check(window.location.href)
  .listen();

  document.body.addEventListener('keypress', (e) => {
    var pagenum = store.pagenum;
    if (e.key === '[' || e.key === 'q') pagenum = restrictPage(store.pagenum - 1);
    if (e.key === ']' || e.key === 'w') pagenum = restrictPage(store.pagenum + 1);
    Router.navigate(`${store.pageroute}/${pagenum}`);
  });

  var app = new Vue({
    el: '#container',
    data: { store },
  });

  return app;

}(Vue));

export var page = {
  index: Number,
  name: String,
  filename: String,
  location: String,
  mtime: Date,
};

export var pages = Array;

export var store = {
  title: String,
  pages,
  pageroute: String,
  pagenum: Number,
  sidebar: Boolean,
};
